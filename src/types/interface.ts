
export interface LayerInfo {
  layer_name: string;
  alias: string;
  visible: boolean;
  color: string;
  min_zoom: number;
  max_zoom: number;
  geometry_type:string;
  schema_name:string;
  renderer_type:string;
  editable:boolean;
  id:number
}